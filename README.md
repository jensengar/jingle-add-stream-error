# jingle-add-stream-error

### Getting started

#### install global dependencies
`npm install -g stupid-server gulp`

#### install dependencies
`npm install`

#### start server
`stupid-server -s`
then go to `https://localhost:8443/tinker.html`